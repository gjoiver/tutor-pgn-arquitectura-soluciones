# <topic_id> · <NN> — <Título>

- Herramienta: <tool>
- Tipo: script | completar | test | debug | predecir
- Tiempo objetivo: <X> min
- Directorio de trabajo: `ejercicios/fase-N/<topic_id>/NN-slug/`

## Por qué esta herramienta
<1-3 líneas: qué problema resuelve y qué se usaba antes>

## Objetivo
<especificación clara de lo que vas a construir: entrada, salida, restricciones>

## Paso a paso
<Pasos numerados y autocontenidos. Cada paso indica el archivo sobre el que se
trabaja (ruta exacta) y, si hay que ejecutar algo, el comando literal. Los pasos
guían el proceso, no la solución. Estructura de referencia:>

0. **Preparación** — qué archivos ya existen aquí y su rol (`starter.py`:
   esqueleto a completar; `test_starter.py`: tests que deben pasar; datos si
   aplica); qué archivos debes CREAR tú y en qué ruta; comandos de setup si
   aplican (crear entorno, instalar dependencias, levantar un lab), listados
   literalmente:
   ```bash
   <comando de setup>
   ```
1. <Primer paso concreto: qué lograr y en qué archivo>
2. <Siguiente paso...>
3. **Verifica**: corre
   ```bash
   <comando de verificación>
   ```
   y confirma que <salida esperada>.

## Convención de código
Variables, funciones, docstrings y comentarios en inglés (estándar del repo).

## Cómo se evalúa
<comando exacto que el tutor ejecutará, p. ej. `pytest test_starter.py -q`, y el criterio>

## Pistas
Pídelas al tutor: son escalonadas y no están escritas aquí a propósito.
